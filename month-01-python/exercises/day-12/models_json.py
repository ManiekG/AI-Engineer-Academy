"""Dzień 12: zapisz i odczytaj listę modeli w formacie JSON."""

import json
from pathlib import Path


def save_models(models: list[dict[str, object]], path: Path) -> None:
    # TODO: json.dump(..., ensure_ascii=False, indent=2)
    raise NotImplementedError
