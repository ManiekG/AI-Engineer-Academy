# Dzień 29 — składanie Document Analyzer API

Połącz elementy z poprzednich dni:

1. endpoint `GET /health`;
2. endpoint `POST /analyze` z Pydantic;
3. upload dokumentu TXT;
4. analiza liczby słów i znaków;
5. logowanie oraz testy.

Wynik: uruchamialne API z README opisującym instalację i przykładowe wywołanie `curl`.
