"""Dzień 18: klient REST z requests.

Pobierz JSON z wybranego publicznego API, dodaj timeout i obsługę błędów HTTP.
"""


def fetch_json(url: str) -> dict[str, object]:
    # TODO: requests.get(url, timeout=10), raise_for_status(), response.json().
    raise NotImplementedError
